# Local values for the Firebase emulator only.
# Copy to `.env.local` and fill in real keys. In production these come from
# Firebase secrets (Google Secret Manager), NOT this file.
GROQ_API_KEY=your_groq_key
NEWS_API_KEY=your_newsapi_key
YOUTUBE_API_KEY=your_youtube_key
