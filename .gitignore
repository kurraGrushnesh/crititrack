# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code
.vscode/

# Claude Code (local scratch + worktrees)
.claude/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
build/
android/build/
android/**/build/
pubspec.lock

# Scratch / experiment files
test_groq*
scratch_*

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# === SECURITY: API Keys — NEVER commit these ===
.env
*.env
!.env.example

# Firebase
# firebase_options.dart holds only public client config (project id, sender id,
# app id, and a Firebase API key that is NOT a secret — access is controlled by
# Security Rules + App Check). It must be committed for the app to initialise.
# google-services.json / GoogleService-Info.plist stay out until Android/iOS CI
# needs them, at which point they are injected from a CI secret.
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# Gradle cache
android/.gradle/

# Docs (Word files)
android/app/*.docx

# Node
node_modules/
/package-lock.json
lib/core/constants/api_keys.dart